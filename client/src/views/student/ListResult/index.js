import React from 'react';
import Timer from '../../../components/Timer';

const ListResult = () => {
  return (
    <div>
      <Timer timeRemain={1658116944000} onComplete={() => {}} />
    </div>
  );
};

export default ListResult;
